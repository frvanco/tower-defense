services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: tower_defense
      POSTGRES_PASSWORD: tower_defense
      POSTGRES_DB: tower_defense
    ports:
      - '55432:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
